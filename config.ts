// Application configuration
export const APP_CONFIG = {
  // GitHub repository URL for contribution links
  GITHUB_REPO_URL: 'https://github.com/voku/MentaleDeveloperMap',
  
  // Base path for GitHub Pages deployment
  // This should match your GitHub repository name
  BASE_PATH: '/MentaleDeveloperMap/',
} as const;
